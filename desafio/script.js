function mostrarAlerta () {
    // Retirar o estilo display:none do alerta-1
}

function mostrarAlertaComShow() {
    // Adicionar classe show no alerta-2
    //Para executar essa função clique no botão "Alerta bonitinho"
}

function desapareceComOAlerta() {
    // Remover a classe show do alerta-2
    // 
}

function adicionar() {
    // Captura os valores dos dois campos ( através da propriedade "value" de um elemento input )
    // E soma-los
    document.getElementById('resultado').innerHTML = '?';
}

function subtrair() {
    // Captura os valores dos dois campos ( através da propriedade "value" de um elemento input )
    // E subtrai-los
    document.getElementById('resultado').innerHTML = '?';
}

function multiplicar() {
    // Captura os valores dos dois campos ( através da propriedade "value" de um elemento input )
    // E multiplica-los
    document.getElementById('resultado').innerHTML = '?';
}

function dividir() {
    // Captura os valores dos dois campos ( através da propriedade "value" de um elemento input )
    // E dividi-los
    document.getElementById('resultado').innerHTML = '?';
}

function elevar() {
    // Captura os valores dos dois campos ( através da propriedade "value" de um elemento input )
    // E elevar a potencia do segundo ( Tentei dar uma googlada, vao precisar usar o objeto "Math" )
    document.getElementById('resultado').innerHTML = '?';
}